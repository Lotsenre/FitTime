<UserControl x:Class="FitTime.Views.UsersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#1A1A1A">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="24,16,24,0">
            <TextBlock Text="ПОЛЬЗОВАТЕЛИ" FontSize="24" FontWeight="Bold" Foreground="White"/>
            <Button Content="+ ДОБАВИТЬ" HorizontalAlignment="Right"
                    Command="{Binding AddUserCommand}"
                    Background="#F4B448" Foreground="Black" FontWeight="Bold" FontSize="13"
                    Height="38" Padding="20,0"/>
        </Grid>

        <!-- Column headers -->
        <Grid Grid.Row="1" Margin="24,16,24,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.5*"/>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="1.5*"/>
                <ColumnDefinition Width="1.5*"/>
                <ColumnDefinition Width="1.5*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="ЛОГИН" FontSize="11" Foreground="#666"/>
            <TextBlock Grid.Column="1" Text="ФИО" FontSize="11" Foreground="#666"/>
            <TextBlock Grid.Column="2" Text="РОЛЬ" FontSize="11" Foreground="#666"/>
            <TextBlock Grid.Column="3" Text="СТАТУС" FontSize="11" Foreground="#666"/>
            <TextBlock Grid.Column="4" Text="СОЗДАН" FontSize="11" Foreground="#666"/>
        </Grid>

        <!-- List -->
        <ListBox Grid.Row="2" Margin="24,8,24,0"
                 ItemsSource="{Binding Users}"
                 SelectedItem="{Binding SelectedUser}"
                 Background="Transparent" BorderThickness="0">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0,1,0,0"/>
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <Border x:Name="Bd" Background="#1E1E1E" Padding="12" BorderThickness="3,0,0,0" BorderBrush="Transparent">
                                    <ContentPresenter/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Bd" Property="Background" Value="#2A2A2A"/>
                                        <Setter TargetName="Bd" Property="BorderBrush" Value="#F4B448"/>
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Bd" Property="Background" Value="#252525"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1.5*"/>
                            <ColumnDefinition Width="3*"/>
                            <ColumnDefinition Width="1.5*"/>
                            <ColumnDefinition Width="1.5*"/>
                            <ColumnDefinition Width="1.5*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="{Binding Login}" Foreground="White" FontSize="14" FontWeight="Medium" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="1" Text="{Binding FullName}" Foreground="White" FontSize="14" FontWeight="Medium" VerticalAlignment="Center"/>

                        <!-- Role badge -->
                        <Border Grid.Column="2" Background="{Binding RoleBgColor}" CornerRadius="2"
                                Padding="8,3" HorizontalAlignment="Left" VerticalAlignment="Center">
                            <TextBlock Text="{Binding RoleName}" Foreground="{Binding RoleColor}" FontSize="10" FontWeight="Bold"/>
                        </Border>

                        <!-- Status badge -->
                        <Border Grid.Column="3" Background="{Binding StatusBgColor}" CornerRadius="2"
                                Padding="8,3" HorizontalAlignment="Left" VerticalAlignment="Center">
                            <TextBlock Text="{Binding StatusText}" Foreground="{Binding StatusColor}" FontSize="10" FontWeight="Bold"/>
                        </Border>

                        <TextBlock Grid.Column="4" Text="{Binding CreatedAt, StringFormat='{}{0:dd.MM.yyyy}'}" Foreground="#555" FontSize="13" VerticalAlignment="Center"/>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Footer -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="24,8,24,16">
            <Button Content="РЕДАКТИРОВАТЬ" Command="{Binding EditTrainerDataCommand}"
                    Style="{StaticResource MaterialDesignFlatButton}" Foreground="White" FontSize="12" Margin="0,0,8,0"
                    IsEnabled="{Binding CanEditSelectedUser}"/>
            <Button Content="СБРОСИТЬ ПАРОЛЬ" Command="{Binding ResetPasswordCommand}"
                    Style="{StaticResource MaterialDesignFlatButton}" Foreground="White" FontSize="12" Margin="0,0,8,0"
                    IsEnabled="{Binding CanEditSelectedUser}"/>
            <Button Content="ЗАБЛОКИРОВАТЬ" Command="{Binding BlockUserCommand}"
                    Style="{StaticResource MaterialDesignFlatButton}" Foreground="#C0392B" FontSize="12"
                    IsEnabled="{Binding CanEditSelectedUser}"/>
        </StackPanel>
    </Grid>
</UserControl>
