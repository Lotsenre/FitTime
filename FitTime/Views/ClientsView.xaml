<UserControl x:Class="FitTime.Views.ClientsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Background="#1A1A1A">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header: search + filters + add button -->
        <Grid Grid.Row="0" Margin="24,16,24,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Search -->
            <TextBox Grid.Column="0"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     materialDesign:HintAssist.Hint="Поиск..."
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     FontSize="14" VerticalAlignment="Center"/>

            <!-- Filters -->
            <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="20,0,0,0" VerticalAlignment="Center">
                <RadioButton Content="ВСЕ" GroupName="ClientFilter" IsChecked="True"
                             Style="{StaticResource MaterialDesignTabRadioButton}"
                             Foreground="#F4B448" FontSize="13" Margin="0,0,8,0"
                             Command="{Binding SetFilterCommand}" CommandParameter="Все"/>
                <RadioButton Content="АКТИВНЫЕ" GroupName="ClientFilter"
                             Style="{StaticResource MaterialDesignTabRadioButton}"
                             Foreground="White" FontSize="13" Margin="0,0,8,0"
                             Command="{Binding SetFilterCommand}" CommandParameter="Активные"/>
                <RadioButton Content="ПРОСРОЧЕННЫЕ" GroupName="ClientFilter"
                             Style="{StaticResource MaterialDesignTabRadioButton}"
                             Foreground="White" FontSize="13" Margin="0,0,8,0"
                             Command="{Binding SetFilterCommand}" CommandParameter="Просроченные"/>
                <RadioButton Content="БЕЗ АБ." GroupName="ClientFilter"
                             Style="{StaticResource MaterialDesignTabRadioButton}"
                             Foreground="White" FontSize="13"
                             Command="{Binding SetFilterCommand}" CommandParameter="Без аб."/>
            </StackPanel>

            <!-- Add button -->
            <Button Grid.Column="2" Content="+ ДОБАВИТЬ"
                    Command="{Binding AddClientCommand}"
                    Background="#F4B448" Foreground="Black"
                    FontWeight="Bold" FontSize="13"
                    Height="38" Padding="20,0"
                    Visibility="{Binding CanEdit, Converter={StaticResource BooleanToVisibilityConverter}}"/>
        </Grid>

        <!-- Column headers -->
        <Grid Grid.Row="1" Margin="24,16,24,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="2.5*"/>
                <ColumnDefinition Width="2.5*"/>
                <ColumnDefinition Width="1.5*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="ФИО" FontSize="11" Foreground="#666"/>
            <TextBlock Grid.Column="1" Text="ТЕЛЕФОН" FontSize="11" Foreground="#666"/>
            <TextBlock Grid.Column="2" Text="EMAIL" FontSize="11" Foreground="#666"/>
            <TextBlock Grid.Column="3" Text="АБОНЕМЕНТ" FontSize="11" Foreground="#666"/>
            <TextBlock Grid.Column="4" Text="РЕГИСТРАЦИЯ" FontSize="11" Foreground="#666"/>
        </Grid>

        <!-- Clients list -->
        <ListBox Grid.Row="2" Margin="24,8,24,0"
                 ItemsSource="{Binding Clients}"
                 SelectedItem="{Binding SelectedClient}"
                 Background="Transparent" BorderThickness="0"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 MouseDoubleClick="ClientsList_MouseDoubleClick">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0,1,0,0"/>
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <Border x:Name="Bd" Background="#1E1E1E" Padding="0,12" BorderThickness="3,0,0,0" BorderBrush="Transparent">
                                    <ContentPresenter/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Bd" Property="Background" Value="#2A2A2A"/>
                                        <Setter TargetName="Bd" Property="BorderBrush" Value="#F4B448"/>
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Bd" Property="Background" Value="#252525"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="12,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="3*"/>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="2.5*"/>
                            <ColumnDefinition Width="2.5*"/>
                            <ColumnDefinition Width="1.5*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{Binding FullName}" Foreground="White" FontSize="14" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="1" Text="{Binding Phone}" Foreground="#CCC" FontSize="14" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="2" Text="{Binding Email}" Foreground="#CCC" FontSize="14" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="3" Text="{Binding MembershipStatus}" Foreground="{Binding MembershipStatusColor}" FontSize="14" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="4" Text="{Binding CreatedAt, StringFormat='{}{0:dd.MM.yyyy}'}" Foreground="#888" FontSize="14" VerticalAlignment="Center"/>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Footer -->
        <Grid Grid.Row="3" Margin="24,8,24,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" VerticalAlignment="Center" Foreground="#666" FontSize="13">
                <Run Text="Показано "/>
                <Run Text="{Binding Clients.Count, Mode=OneWay}"/>
                <Run Text=" из "/>
                <Run Text="{Binding TotalCount, Mode=OneWay}"/>
                <Run Text=" клиентов"/>
            </TextBlock>

            <Button Grid.Column="1" Content="РЕДАКТИРОВАТЬ"
                    Command="{Binding EditClientCommand}"
                    Style="{StaticResource MaterialDesignFlatButton}"
                    Foreground="White" FontSize="12" Margin="0,0,8,0"/>
            <Button Grid.Column="2" Content="ДЕАКТИВИРОВАТЬ"
                    Command="{Binding DeactivateClientCommand}"
                    Style="{StaticResource MaterialDesignFlatButton}"
                    Foreground="White" FontSize="12" Margin="0,0,8,0"/>
            <Button Grid.Column="3" Content="ОТКРЫТЬ ПРОФИЛЬ"
                    Command="{Binding OpenProfileCommand}"
                    Background="#F4B448" Foreground="Black"
                    FontWeight="Bold" FontSize="12"
                    Height="36" Padding="16,0"/>
        </Grid>
    </Grid>
</UserControl>
